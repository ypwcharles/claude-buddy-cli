{
  "name": "claude-buddy-cli",
  "version": "0.1.0",
  "description": "CLI to search Claude Code buddy pets and apply matching user IDs.",
  "type": "module",
  "bin": {
    "claude-buddy": "./dist/bin.js"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.1"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run"
  },
  "keywords": [
    "claude",
    "buddy",
    "cli",
    "claude-code"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
